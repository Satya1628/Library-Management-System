# Library-Management-System
A CLI-based Library Management System built using Python and SQLite. It supports book management, member handling, and loan tracking with relational database design. The project demonstrates SQL operations, data integrity, and basic system-level logic.
